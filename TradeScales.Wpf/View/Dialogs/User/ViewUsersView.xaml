<controls:MetroWindow x:Class="TradeScales.Wpf.View.Dialogs.ViewUsersView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"        
        Title="Users"
        Width="600"
        Height="600"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        GlowBrush="{DynamicResource AccentColorBrush}"
        ShowIconOnTitleBar="True"
        SaveWindowPosition="True"
        WindowTransitionsEnabled="False"
        NonActiveWindowTitleBrush="LightGray">

    <!--Window Input Bindings-->
    <Window.InputBindings>
        <KeyBinding Command="{Binding CancelCommand}" Gesture="ESC"/>
        <KeyBinding Command="{Binding CancelCommand}" Gesture="Enter"/>
    </Window.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <!--Users-->
        <DataGrid x:Name="datagrid_Users" Grid.Row="0" ItemsSource="{Binding Users}" AutoGenerateColumns="False" Margin="10" CanUserAddRows="False" VerticalScrollBarVisibility="Visible">
            <DataGrid.Columns>
                <DataGridTextColumn Header = "Username" Width="*" Binding = "{Binding Username}" />
                <DataGridTextColumn Header = "Email" Width="*" Binding = "{Binding Email}" />
                <DataGridTemplateColumn Header="Actions" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Button x:Name="button_Edit"
                                    Command="{Binding ElementName=datagrid_Users, Path=DataContext.EditCommand}" 
                                    CommandParameter="{Binding ElementName=datagrid_Users, Path=SelectedItem}" 
                                    Margin="5">Edit</Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!--Close-->
        <Button Grid.Row="1" Width="50" Margin="10" Content="Close" Command="{Binding CancelCommand}" />
    </Grid>
</controls:MetroWindow>
